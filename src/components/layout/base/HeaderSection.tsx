import React from "react";
import styled from "styled-components";

const HeaderSection = () => {
  return <Header>HJEADER</Header>;
};

const Header = styled.header`
  position: sticky;
  top: 0;
`;

export default HeaderSection;
